{
  "name": "youtube-transcript-actor",
  "version": "0.1.0",
  "description": "Extract clean YouTube transcripts without official APIs",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "dependencies": {
    "apify": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
